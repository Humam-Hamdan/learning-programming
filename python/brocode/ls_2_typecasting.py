name = "name"
age = 24
gpa = 3.2
is_student = True

# to print types
# print(type(name))

# rounds the number
gpa_int = int(gpa)

# adds the comma zero
age_float = float(age)

# behaves like strings
age_str = str(age)

# is false when the str is empty -> check for empty strings.
name_bool = bool(name)

